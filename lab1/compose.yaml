services:
  postgres:
    image: postgres:18.1
    container_name: architecture_postgres
    environment:
      POSTGRES_DB: architecture
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
      TZ: Europe/Kyiv
    ports:
      - "5432:5432"
    volumes:
      - architecture_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U user -d architecture"]
      interval: 5s
      timeout: 3s
      retries: 20

volumes:
  architecture_data:
