databaseChangeLog:
  - changeSet:
      id: 017-add-password_hash-column-to-users
      author: admin
      changes:
        - sql:
            dbms: postgresql
            sql: |
              ALTER TABLE users
                            ADD COLUMN password_hash VARCHAR(255);